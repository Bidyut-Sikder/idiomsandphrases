import React from 'react';
import { StyleSheet, View ,Text} from 'react-native';

const ShowLikedData = () => {
    return (
        <View>
            <Text>Your liked data will be displayed here.</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default ShowLikedData;
